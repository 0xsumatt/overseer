# Nothing auto-loads this file (config.py reads plain os.environ): export these
# from your shell/systemd unit, or run via `uv run --env-file .env ...`.

DATABASE_URL=
DISCORD_WEBHOOK_URL=
FLASK_SECRET_KEY=
OVERSEER_SYMBOLS_FILE=symbols.toml
